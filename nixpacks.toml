# Configure nixpacks for a Python + Node.js project
[phases.setup]
nixPkgs = ["nodejs-18_x", "python311"]

[phases.install]
cmds = ["npm install", "pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8080}"
